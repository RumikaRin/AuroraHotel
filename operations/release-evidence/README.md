# Aurora Hotel release evidence

The canonical Aurora Hotel P0 evidence bundle is:

- `aurora-p0-final/`

The `agent-os-v2-0.1.0/` directory is retained only as historical,
non-canonical runner output from the Template/Agent OS workflow. It must not be
used to attest an Aurora Hotel release.
