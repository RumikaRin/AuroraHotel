# privacy security contract

Deny by default. Validate every external input, authorize at the server boundary, redact sensitive values, and add capability-specific negative tests before production use.
